[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# 🚫 Disable the Next.js plugin since it conflicts with SSR + wallet hooks
[[plugins]]
  package = "@netlify/plugin-nextjs"
  enabled = false
